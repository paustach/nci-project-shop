var stripe = Stripe('Rails.application.credentials[:stripe][:secret]');
var checkoutButton = document.getElementById('checkout-button');

stripe.redirectToCheckout({
    sessionId: '<%= @session.id %>'
  }).then(function (result) {
  });