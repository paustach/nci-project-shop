<p style="color: green"><%= notice %></p>


<% if current_user.try(:admin?) %> 
  <div class="container">
    <div class="row">
      <div class="col-md-2">
        <%= link_to "Back to products", products_path, class:"btn-admin" %>
      </div>
      <div class="col-md-2">
        <%= link_to "Edit this product", edit_product_path(@product), class: "btn-admin" %> 
      </div>
      <div class="col-md-2">
        <%= button_to "Delete this product", @product, method: :delete, class: "btn-admin" %>
      </div>
    </div>
  </div>
    
    
  </div>
<% end %>
<%= render @product %>