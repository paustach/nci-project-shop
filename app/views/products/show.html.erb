<p style="color: green"><%= notice %></p>


<% if current_user.try(:admin?) %> 
  <div class="container">
    <div class="row">
      <div class="col-md-2">
        <%= link_to "Back to all", products_path, class:"btn btn-admin" %>
      </div>
      <div class="col-md-2">
        <%= link_to "Edit product", edit_product_path(@product), class: "btn btn-admin" %> 
      </div>
      <div class="col-md-2">
        <%= button_to "Delete product", @product, method: :delete, class: "btn btn-admin" %>
      </div>
    </div>
  </div>
    
    
  </div>
<% end %>
<%= render @product %>